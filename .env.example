# Supabase
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Resend
RESEND_API_KEY=
RESEND_FROM_EMAIL=

# Expo Push (placeholder)
EXPO_PUSH_TOKEN=

# Stripe (placeholder)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Notification dispatcher (POST /api/notifications/dispatch — optional cron e.g. daily)
# Uses RESEND_API_KEY + RESEND_FROM_EMAIL when set; still writes notification_log for mobile.
NOTIFICATION_LOOKAHEAD_HOURS=24

# Member web hub — App Store / Play Store links (shown prominently on /member)
NEXT_PUBLIC_GATHER_IOS_APP_URL=
NEXT_PUBLIC_GATHER_ANDROID_APP_URL=
